extends layout.pug

block content      
    section#content
        h2= prompt
        if hint
            p
                i Hint: #{hint}
        else
            p There is no hint for this card... maybe because it is too easy!
            